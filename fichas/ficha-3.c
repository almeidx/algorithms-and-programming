#include <stdio.h>

int main() {
    // Exercicio 1

    // int num;
    // printf("Introduza um numero natural");
    // scanf("%d", &num);

    // int mult;
    // do {
    //     mult += num;
    //     printf("%d\n", mult);
    // } while (mult < 100);

    // Exercicio 2

    // int ni, nf;

    // printf("Introduza os numeros inicial e final");
    // scanf("%d%d", &ni, &nf);

    // // <condition> ? <if true> : <if false>
    // int n = ni % 2 == 0 ? ni : ni + 1;
    // do {
    //     printf("%d\n", n);
    //     n += 2;
    // } while (n < nf);

    // Exercicio 3

    // int n;

    // printf("Introduza um numero natural");
    // scanf("%d", &n);

    // int p = 0, total = 0;

    // while (total + p + 2 <= n) {
    //     p += 2;  // p = p + 2;
    //     total += p;
    //     printf("%d\n", p);
    // }

    // printf("total %d", total);

    // Exercicio 4

    // int n;
    // do {
    //     printf("Introduza um numero entre 0 e 9\n");
    //     scanf("%d", &n);
    // } while (n < 0 || n > 9);

    // Exercicio 5

    // float n;
    // do {
    //     printf("Introduza um numero que pertenca a [10, 15.5]\n");
    //     scanf("%f", &n);
    // } while (n < 10.0f || n > 15.5f);

    // Exercicio 6

    // int n, even = 0, uneven = 0;

    // do {
    //     printf(
    //         "Introduza um numero natural. Se quiser terminal, introduza um nº
    //         " "negativo.");
    //     scanf("%d", &n);
    //     if (n % 2 == 0)
    //         even++;
    //     else if (n > 0)
    //         uneven++;
    // } while (n >= 0);

    // printf("Introduziu %d nº pares e %d impares", even, uneven);

    // Exercicio 7

    // int n, m;

    // printf("Introduza o numero base e a quantidade de multiplos.");
    // scanf("%d%d", &n, &m);

    // for (int i = 1; i <= m; i++) {
    //     printf("%d\n", i * n);
    // }
}
